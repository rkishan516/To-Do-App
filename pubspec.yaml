name: to_do
description: A new Flutter project.

version: 1.0.0+1

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^0.1.2
  sqflite: ^1.1.6+3
  path_provider: any
  provider: any
  intl: ^0.15.7

dev_dependencies:
  flutter_test:
    sdk: flutter
flutter:
  uses-material-design: true